# 모드 전환 시나리오

> 상태: 초안 · 최종수정: 2026-09-01
> 관련 기획: [[00_시작/도메인모델]] §5 · [[01_기획/구분자_기획]] 변경·삭제 규칙 · [[01_기획/담보_기획]]

## 시나리오 1 — 프로덕션: 파괴적 변경은 서버가 거부
**전제**: 프로덕션 모드. 담보 「수술비」(세부보장 1종수술~7종수술)가 상품에 탑재되어 있음.
**흐름**:
1. 담보 마스터에서 세부보장 삭제 등 구조 변경 시도 → 서버가 거부, 사유 반환.
2. 화면을 우회해 API 를 직접 호출해도 동일하게 거부된다.
**기대 결과**: 잠금은 화면 숨김이 아니라 서버 검증이다 — 숨김 ≠ 잠금.
**경계·오류**: 파괴적이지 않은 작업은 프로덕션에서도 허용 — 표시명 변경, enum 값 추가, 구분자 값 입력·수정.

## 시나리오 2 — 개발 모드: 구조 추가 → 기존 상품담보에 빈 값
**전제**: 개발 모드. 수술비 담보가 탑재된 상품 존재.
**흐름**:
1. 담보 마스터에 세부보장 「8종수술」 추가 → 허용.
2. 이미 탑재된 상품담보에 추가 노드의 값 자리가 빈 값(미입력)으로 나타난다.
3. 마스터 기본값은 자동 복사되지 않는다 — 탑재 시점 스냅샷 외의 두 번째 복사 시점을 만들지 않는다.
4. 새 값 자리는 완결성 조회에 미입력으로 잡힌다.
**기대 결과**: 구조 변경이 기존 상품담보에 확인 안 된 값을 흘려 넣지 않는다.
**경계·오류**: 미입력을 채우기 전 조립 → 오류 + 좌표 보고.

## 시나리오 3 — 개발 모드: 구조 삭제 → 값 행 연쇄 삭제
**전제**: 개발 모드. 삭제 대상 세부보장이 상품담보 값 행을 갖고, 문면 반복문·조건식이 그 트리를 순회·참조 중.
**흐름**:
1. 세부보장 삭제 시도 → 참조 목록(식·문면 사용처)을 보여주고 확인을 받는다.
2. 확인 후 삭제 → 상품담보의 해당 값 행이 함께 삭제된다.
3. 남은 깨진 참조는 조용히 사라지지 않고 오류 상태로 표시된다.
**기대 결과**: 삭제의 파장이 전부 드러난다 — 조용한 파손 없음.
**경계·오류**: 최소 구조를 위반하는 삭제(담보의 마지막 세부보장, 세부보장의 마지막 급부) → 개발 모드에서도 거부.

## 시나리오 4 — 모드 전환 자체
**전제**: 개발 모드에서 구조 작업을 마친 상태.
**흐름**:
1. 프로덕션 모드로 전환.
2. 이후 파괴적 변경(구조 변경·사용 중 삭제·enum 값 삭제)은 시나리오 1 규칙으로 거부된다.
**기대 결과**: 모드가 파괴적 변경 허용 여부를 가르는 유일한 스위치다.
**경계·오류**: ⚠ 미결: 모드의 단위 — 시스템 전역인가, 환경(인스턴스·데이터셋) 단위인가. ⚠ 미결: 전환 권한과 절차 — 누가 어떤 화면에서 전환하는가. ⚠ 미결: 전환 전 검사 — 깨진 참조(오류 상태)가 남아 있어도 프로덕션 전환이 가능한가.
