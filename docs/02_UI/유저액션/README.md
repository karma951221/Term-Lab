# 유저액션 명세

> 상태: 초안 · 최종수정: 2026-09-02

사람이 이 시스템에서 **할 수 있는 일**의 전수 목록. 화면이 아직 없어도 성립하도록
**의도(커맨드) 단위**로 적는다. 작성 원칙의 근거는 [[06_결정/ADR-0018-유저액션명세-작성원칙]].

## 무엇이 여기 있고 무엇이 없나

| 여기 있는 것 | 여기 없는 것 (있어야 할 곳) |
|---|---|
| 사람이 일으키는 행위 하나하나 · 전제 · 결과 · 거부 조건 | **왜 그런 규칙인가** → `01_기획/` |
| 조회 액션 (바꾸는 것 없이 보는 것) | **대표 흐름·기대결과 서사** → `05_QA/시나리오/` |
| 서버가 거부하는 조건 (모드·중복·참조 무결성) | **레이아웃·컴포넌트·픽셀** → `02_UI/화면기획` |
| 근거 문서로의 링크 | **결정의 맥락·버린 대안** → `06_결정/` ADR |

한 문장을 쓰면서 "이건 흐름인데" 싶으면 그건 시나리오 몫이다.
**명세는 목록이지 서사가 아니다.**

## 파일 구성

기획 영역(feature) 하나 = 파일 하나. 객체는 파일 안의 `##` 섹션 단위다.

| 파일 | 대응 기획 | 다루는 객체 | 액션 | 상태 |
|---|---|---|---|---|
| [[구분자_액션]] | [[01_기획/구분자_기획]] (P1) | 구분자 정의 · 구조체 필드 · enum 유효값 · 값 자리 · 파생식 · const 구분자 · 모드 | 52 | 초안 |
| [[담보_액션]] | [[01_기획/담보_기획]] (P2) | 담보 마스터 · 세부보장 · 급부 · 구분자 부착 · 담보 레벨 값 | 31 | 초안 |
| [[공용조항_액션]] | [[01_기획/공용조항_기획]] (P3) | 공용조항 템플릿 · 옵션 · 참조 · 옵션 오버라이드 | 46 | 초안 |
| [[문면_액션]] | [[01_기획/문면_기획]] (P4) | 문면 문서 · 노드 트리 · 조건/반복/슬롯 · 조연결 · 별표 마스터 · 사전평가 | 83 | 초안 |
| [[상품탑재_액션]] | [[01_기획/상품탑재_기획]] (P5) | 상품 · 담보속성 · 상품담보(탑재) · 세목유형 · 세목 선택지 · 상품세목 · 기본계약 · 상품 레벨 값 | 57 | 초안 |
| [[조립_액션]] | [[01_기획/조립_기획]] (P6) | 특약 그룹 · 조립 실행 · 오류 좌표 · 미리보기 · 별표 번호 | 27 | 초안 |

초안 일괄 작성 2026-09-02 — 6영역 36객체 · 액션 296건.

## 항목 형식

객체 하나 = `##` 섹션 하나. 섹션마다 표 하나, 액션 하나 = 행 하나.

```markdown
## <객체명>

> 대응 기획: [[01_기획/<주제>_기획]] · 대응 시나리오: [[05_QA/시나리오/<흐름>_시나리오]]

| 액션 | 전제 | 결과 | 거부 | 근거 |
|---|---|---|---|---|
| 구분자 채번 | 코드·표시명·타입 입력 | 카탈로그에 정의 1건 추가 | 코드 중복 시 채번 시점 거부 | [[구분자_기획]] · S2 |

**체크리스트** — 생성 ✓ · 조회 ✓ · 수정 ✓ · 삭제 ✓ · 관계 맺기 ✓ · 관계 끊기 ✓ ·
순서 변경 해당없음(순서 개념 없음) · 상태 전이 ✓ · 복제 해당없음(채번이 곧 신규)

> 진입경로(잠정): 구분자 카탈로그 화면
```

각 열이 담는 것:

- **액션** — 도메인 용어 + 동사. 용어는 [[00_시작/용어사전]] 을 따른다.
- **전제** — 이 액션이 가능하려면 있어야 하는 상태. 모드 제약도 여기.
- **결과** — 저장소에 **무엇이 바뀌나**. 조회 액션이면 `바뀜 없음 — <무엇이 보이나>`.
- **거부** — 서버가 막는 조건. 「화면에서 숨김」은 거부가 아니다 ([[도메인모델]] §5).
- **근거** — `[[기획]]` · `[[ADR]]` · `[[시나리오]]` 위키링크. 없으면 비운다(= 근거 없는 액션 신호).

`진입경로(잠정)` 은 화면기획이 생기면 지운다 — 그때부터 정본은 화면기획이다.

## 완전성 검사

**① 체크리스트 훑기** — 객체마다 아래 9종을 전부 훑고, 해당 없으면 **「해당없음 — 이유」로 명시**한다.
그냥 빠뜨리는 것을 금지한다.

`생성` · `조회` · `수정` · `삭제` · `관계 맺기(부착·참조·탑재)` · `관계 끊기` ·
`순서 변경` · `상태 전이(모드·확정·스냅샷)` · `복제`

**② 시나리오 역방향 대조** — [[05_QA/시나리오]] 13개의 모든 단계가 이 명세의 어떤 액션 하나에
대응해야 한다.
- 대응할 액션이 없는 시나리오 단계 = **명세 누락**
- 대응하는 시나리오도 기획 근거도 없는 액션 = **유령 기능** (지우거나 기획으로 올린다)

## 임의 결정 (검토 대상)

초안은 기획에 답이 없는 자리를 **묻지 않고 임의로 정해서** 채웠다. 확정 대화의 입력물이다.
각 파일 끝의 `## 임의 결정` 표에 모아 두고, 본문 해당 행에는 `〔D-P1-3〕` 처럼 마커를 단다.

| 파일 | 임의 결정 수 |
|---|---|
| 구분자_액션 | 15 (D-P1-1 ~ 15) |
| 담보_액션 | 13 (D-P2-1 ~ 13) |
| 공용조항_액션 | 12 (D-P3-1 ~ 12) |
| 문면_액션 | 25 (D-P4-1 ~ 25) |
| 상품탑재_액션 | 15 (D-P5-1 ~ 15) |
| 조립_액션 | 14 (D-P6-1 ~ 14) |
| **계** | **94** |

확정된 임의 결정 중 대안이 갈렸던 것은 `06_결정/` ADR 로 올린다.

## 확정 대화 대기 (2026-09-02 초안이 남긴 것)

영역을 가로지르는 쟁점이라 파일 하나로는 못 닫는 것들. 티키타카 순서의 후보다.

### 명세 누락 후보 (액션이 어디에도 없다)

| # | 내용 | 걸린 시나리오 |
|---|---|---|
| 1 | **깨진 참조를 고치는 액션이 없다** — 조회로 보여주기만 하고 해소·재연결 경로가 P1~P6 어디에도 없다. 개발→프로덕션 전환 차단과 직결 | 구분자정의 S6-3 · 모드전환 S3-3 |
| 2 | **재검사 목록에서 직접 일괄 부착하는 경로**가 없다 — 목록만 보여주고 담보 화면으로 돌려보낼지 | 공용조항 S3 |
| 3 | **두 조립 결과를 나란히 대조하는 액션**이 없다 — 결과를 저장하지 않는 「매번 재계산」과 부딪힌다 | 그룹핑별표 S4-3 |
| 4 | **중복 채번 시도를 세목·파생으로 유도하는 장치**가 MVP 에 없다 — 기획상 「규칙의 눈 검증」인데 인수기준 S4 가 여기 걸려 있다 | 체증체감납 S2 |

### 정본 위치 미정 (액션이 두 파일에 걸쳐 있다)

- **기본계약 지정 시 부착 검사** — P3·P5 에 의도적으로 중복 기재 (D-P3-11)
- **공용조항 부착 제안 수락 → 그 자리에서 구분자 부착** — P2·P3 사이 미정 (문면작성 S5-3)

객체 축을 택한 대가다 ([[06_결정/ADR-0018-유저액션명세-작성원칙]] 「감수하는 비용」).

### 영역 간 엇갈린 임의 결정

- **복제 정책** — P3 는 복제 지원(같은 본문 재작성 방지, D-P3-3), P1·P5 는 복제 금지
  (중복 채번 유도·두 번째 값 복사 시점, D-P1 / D-P5-6). 이유는 각각 타당한데
  원칙으로 정리할지 볼 자리.
- **되돌리기 경로** — 「값 지우기」(D-P1)·「값 비우기」(D-P2)가 같은 것을 다른 이름으로
  신설했다. 이름과 소유 객체를 하나로 맞춰야 한다.

### QA 보강 후보

- **반복(for) 전용 시나리오가 없다** — 세부보장 반복은 MVP 필수인데 [[05_QA/시나리오]] 에
  대응 흐름이 없다.
