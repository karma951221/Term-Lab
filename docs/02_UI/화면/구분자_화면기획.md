# 구분자 화면기획

> 상태: 초안 · 최종수정: 2026-09-06
>
> 대응 기획: [[01_기획/구분자_기획]] · 대응 액션: [[02_UI/유저액션/구분자_액션]] · 레이아웃: [[02_UI/디자인원칙]] §2 L1

구분자 탭의 화면 전부를 여기서 다룬다. 2026-09-06 에 **조회 화면** 과 **상세 · 수정 화면** 에 QA 를 걸었다.
생성은 아직 뼈대다.

## 1. 조회 화면 (`/catalog`)

### 목적

카탈로그의 구분자 정의를 훑고, 조건으로 좁혀서, 하나를 골라 상세로 들어간다.
사용자는 **현업 담당자**다 — 화면의 단어는 개발 용어가 아니라 업무 용어여야 한다.

### 화면 구성

```
┌──────────────────────────────────────────────────────────┐
│ 구분자                                              [+]   │  제목 줄 · 오른쪽 끝에 「새 구분자」 아이콘 버튼
├──────────────────────────────────────────────────────────┤
│ 🔍 코드 · 표시명            기준일 [2026-09-06] □미확정 포함│  검색 + 이력일자 (sticky)
│ 종류: 폼 ✕   레벨: 담보 ✕                                  │  걸린 필터 칩 (없으면 줄 자체가 없음)
├──────┬────────────────┬────────┬────────┬────────────────┤
│ 코드 │ 표시명          │ 종류 ▾ │ 레벨 ▾ │ 노출 ▾          │  컬럼 헤더에서 필터
├──────┴────────────────┴────────┴────────┴────────────────┤
│ 총 N건                            (여러 페이지일 때만) ‹ › │  하단 고정
└──────────────────────────────────────────────────────────┘
```

**제목 줄**

- 오른쪽 끝에 **`+` 아이콘 버튼** 하나. tooltip 「새 구분자」(§1.6 — 아이콘 버튼은 tooltip 의무).
  텍스트 링크 「+ 새 구분자」 였던 것을 바꾼다.
- **「enum 목록」 버튼은 두지 않는다.** 선택지(enum)와 구조체는 별도 탭 [[유형_화면기획]] 이 관리한다
  (2026-09-06 확정). 구조체(화면 라벨 「폼」)는 여전히 구분자라 이 목록에 종류 = 폼으로 잡히지만, 필드 편집은 유형 탭이 정본이다.

**검색 + 이력일자 바** (sticky, §2 L1)

- **검색창** 하나. 코드 · 표시명 부분 일치. **「적용」 버튼 없음** — 입력하면 표가 따라 바뀐다.
  - 타이핑은 **디바운스 300ms** (마지막 키 입력 뒤 300ms 지나면 반영). 키마다 표를 다시 그리면 IME(한글 조합)
    중간 상태로 필터가 걸려 표가 깜빡인다. 쓰로틀보다 디바운스 — 조합이 끝난 최종 문자열 하나만 반영하면 된다.
  - 반영된 검색어는 URL 쿼리(`?q=`)에도 실린다 — 뒤로가기 · 링크 공유가 필터 상태를 잃지 않게. 히스토리는 `replace`
    (키 입력마다 히스토리 항목이 쌓이지 않게).
  - 선택형 필터(종류 · 레벨 · 노출)와 기준일은 **디바운스 없이 즉시** 반영한다 — 고르는 행위가 곧 확정이다.
- **기준일(이력일자)** 선택. 기본 = 오늘. [[06_결정/ADR-0026-이력개정-이력일자-작업본확정]] 결 1 —
  「조회 · 생성 화면에 기준일 선택 UI (기본 = 오늘)」. 표는 기준일 이하 최신 확정 행을 보인다.
- **「미확정 포함」 토글**. 기본 꺼짐. 켜면 미적용(미확정 작업본) 레코드도 잡힌다 (ADR-0026 결 3).
- 이력 자체는 MVP 이후 구현이지만(ADR-0026), **화면 자리는 지금 잡는다** — 나중에 필터바를 다시 짜지 않게.
  구현 전까지는 기준일 = 오늘로 고정 표시, 조작 불가(잠긴 필드 — §1.2 출처 문법).

**필터 칩 줄**

- 컬럼 필터가 하나라도 걸리면 검색 바 아래에 **칩**으로 나열한다 — `종류: 폼 ✕`. 검색어는 검색창에
  그대로 보이므로 칩으로 만들지 않는다.
- 칩 오른쪽 끝 **✕** 로 그 필터 하나만 푼다. 칩이 0개면 줄을 그리지 않는다 (빈 줄이 필터바 높이를 먹지 않게).
- 칩이 있는 동안 검색 바 오른쪽 끝에 **「모두 지우기」** 텍스트 버튼. 칩 하나뿐일 때는 안 보인다 (✕ 와 같은 일).

**컬럼 헤더 필터** — 상단의 종류 · 레벨 셀렉트를 없애고 **각 컬럼 헤더에서 건다.**
§2 L1 「필터 항목은 표의 컬럼과 일치시킨다」를 배치로도 지키는 것이다.

| 컬럼 | 필터 | 비고 |
|---|---|---|
| 코드 | 검색창이 담당 | 헤더 필터 없음 |
| 표시명 | 검색창이 담당 | 헤더 필터 없음. 남는 폭을 먹는 유일한 컬럼 (§2 L1) |
| 종류 | 헤더 `▾` → 값 하나 선택 | 단일값 · 폼 · 상수 · 파생 (명칭은 아래) |
| 레벨 | 헤더 `▾` → 값 하나 선택 | 상품 · 세목 · 담보 · 세부보장 · 급부. 상수는 레벨이 없으므로 레벨 필터를 걸면 빠진다 |
| 노출 | 헤더 `▾` → 무조건 · 선택 | 단일값 · 폼만 값이 있다. 나머지는 「—」 |

- 헤더 `▾` 를 누르면 그 컬럼의 값 목록이 팝오버로 뜬다. 하나를 고르면 즉시 반영 + 칩 생성 + 팝오버 닫힘.
- 필터가 걸린 컬럼은 헤더의 `▾` 가 채워진 형태(`▼`)로 바뀐다 — 칩을 못 봐도 헤더만으로 「여기 걸려 있다」가 보인다.
- 컬럼당 값 하나(단일 선택). 복수 선택은 지금 필요가 없다 — 필요해지면 그때 결정.

**종류 명칭 — 「스칼라」 · 「구조체」를 쓰지 않는다** (2026-09-06 확정)

| 도메인 kind | 화면 라벨 (변경 전) | 화면 라벨 (변경 후) |
|---|---|---|
| `scalar` | 스칼라 | **단일값** |
| `struct` | 구조체 | **폼** |
| `const` | 상수 | 상수 |
| `derived` | 파생 | 파생 |

「스칼라」 · 「구조체」는 개발자 용어다. 단일값은 「값 하나를 묻는 구분자」, 폼은 「값 여러 개를 한 번에 묻는 구분자」로
현업에게 바로 읽힌다 — 용어사전의 「폼 하나 = 구조체 타입 구분자 하나」를 화면 라벨로 옮긴 것이다. 「일반」은 폼이 아닌 것으로
읽히기엔 흐려서 버렸다. 문서 · 코드의 도메인 용어는 그대로 「구조체」 다. 라벨은 `src/app/_lib/labels.ts` 한 곳에서 바꾼다
(§9 — 영문 값이 화면에 새지 않게 라벨 맵 한 곳만 통과).

**표**

- 컬럼: 코드 · 표시명 · 종류 · 레벨 · 노출. 2026-09-06 현재 구현과 같다.
- 1열 코드 mono 104px, 표시명이 남는 폭, 나머지는 고정폭 (§2 L1).
- 표시명 클릭 → 상세(§2).

**하단 고정 줄**

- **총 N건** 은 항상 보인다 (몇 건 중 몇 번째인지가 늘 보여야 한다 — §2 L1).
- **‹ 이전 · 다음 ›** 는 **페이지가 둘 이상일 때만** 그린다. 한 페이지면 조작할 것이 없는 버튼을 보여 주지 않는다.
- 여러 페이지에서 첫 페이지면 「이전」 비활성, 마지막 페이지면 「다음」 비활성. `p/P` 표시는 여러 페이지일 때만.
- 페이지 크기 50. 필터를 바꾸면 1페이지로 돌아간다.

### 조작과 상태 전이

| 조작 | 결과 |
|---|---|
| `+` 클릭 | 생성 화면(§3)으로. 조회에 걸린 필터(종류 · 레벨)가 생성 폼의 초기값으로 넘어간다 (§9.1 스마트 디폴트) |
| 검색어 입력 | 300ms 뒤 표 갱신 · URL `?q=` 갱신 · 1페이지로 |
| 헤더 `▾` 에서 값 선택 | 즉시 표 갱신 · 칩 추가 · 헤더 `▼` · 1페이지로 |
| 칩 ✕ | 그 필터 해제 · 표 갱신 · 헤더 `▾` 복귀 |
| 모두 지우기 | 컬럼 필터 전부 해제. 검색어 · 기준일은 유지 |
| 기준일 변경 | 즉시 표 갱신 (이력 구현 후) |
| 미확정 포함 토글 | 즉시 표 갱신 (이력 구현 후) |
| 표시명 클릭 | 상세(§2) |
| 이전 · 다음 | 페이지 이동. 필터 · 검색어 · 기준일 유지 |

URL 이 상태의 정본이다 — `?q=&kind=&level=&exposure=&asOf=&draft=&page=`. 새로고침 · 뒤로가기 · 링크 공유가 같은 화면을 준다.

### 예외 · 빈 상태 · 오류

- **카탈로그 자체가 비었을 때**: 표 대신 EmptyState — 구분자가 무엇인지 한 줄 + 예시 + 「새 구분자 만들기」 (§9.3).
- **필터 탓에 0건**: 「이 조건에 맞는 구분자가 없습니다」 한 줄 + 칩은 그대로 (무엇을 풀면 되는지 보이게).
- **검색어가 조합 중**(IME): 디바운스가 흡수한다. 조합 중간 문자열로 0건이 잠깐 뜨는 것을 막는다.

### 2026-09-06 QA 반영 목록

| # | QA | 반영 |
|---|---|---|
| 1 | 새 구분자 추가 버튼을 `+` 아이콘으로, 배치 수정 | 제목 줄 오른쪽 끝 · tooltip 「새 구분자」 |
| 2 | 「enum 목록」 버튼의 필요 이유를 모르겠음 | 조회 화면에서 제거. 선택지 · 구조체는 [[유형_화면기획]] 탭으로 (Q-A 닫힘) |
| 3 | 「적용」 버튼 제거 · 입력 즉시 반영 · 디바운스/쓰로틀 고민 | 검색어 디바운스 300ms · 선택형은 즉시 · URL replace |
| 4 | 종류의 「스칼라」 명칭이 현업에게 이상함 | 「단일값」 · 구조체는 「폼」 (Q-B 닫힘) · labels.ts 한 곳 |
| 5 | 종류 · 레벨 필터는 컬럼 쪽이 적절 · 칩 + ✕ | 헤더 `▾` 필터 · 칩 줄 · 모두 지우기 |
| 6 | 1페이지인데 이전 · 다음 노출 | 여러 페이지일 때만 · 양 끝 비활성 |
| 7 | 이력일자 선택 버튼 없음 | 기준일(기본 오늘) + 미확정 포함 토글 — ADR-0026 |

### 인수기준
> [[05_QA/인수기준]] 에 등록 (미등록)

- 검색창에 「갱신」 을 치면 버튼 없이 300ms 안에 표가 좁혀지고 URL 에 `q=갱신` 이 실린다.
- 종류 헤더에서 「폼」 을 고르면 칩 `종류: 폼 ✕` 가 생기고, ✕ 를 누르면 원래 표로 돌아온다.
- 총 건수가 페이지 크기 이하면 이전 · 다음이 화면에 없다.
- 화면 어디에도 「스칼라」 · 「구조체」 · 「enum」 · `scalar` 같은 개발 용어가 보이지 않는다.

## 2. 상세 · 수정 화면 (`/catalog/<code>`)

> 2026-09-06 QA 반영. 편집 흐름은 [[02_UI/디자인원칙]] §2 L2 「편집 흐름」 — 읽기 모드가 기본, 우측 상단 「편집」 → 「저장」.

### 목적

구분자 하나를 한눈에 보고, 필요하면 고친다. 어디서 쓰이는지가 같은 화면에 있어야 「고쳐도 되나」를 판단한다.

### 화면 구성 — 좌우 2열

```
┌──────────────────────────────────────────────────────────────┐
│ 갱신여부  D0007                                [✕] [편집] [🗑] │  헤더: 표시명 · 코드 · 우측 상단 조작
├───────────────────────────────┬──────────────────────────────┤
│ 종류      단일값               │ 사용처  3 / 전체 참조 128      │
│ 레벨 ⓘ   담보                  │ ┌────────┬─────────────────┐ │
│ 표시명   [갱신여부         ]   │ │ 형태    │ 좌표             │ │
│ 설명     [                 ]   │ │ 조건식  │ 수술비 §3 ①      │ │
│ 노출 ⓘ   ☑ 무조건              │ │ 치환슬롯│ …               │ │
│ 타입     예/아니오             │ └────────┴─────────────────┘ │
│ 기본값 ⓘ [예 ▾]                │                              │
└───────────────────────────────┴──────────────────────────────┘
```

- **왼쪽 = 기본 정보** 한 그리드 (라벨 120px + 값, §2 L2). 섹션 제목 없이 바로 필드. 「기본 정보」 · 「사용처」 · 「위험 구역」
  으로 세로로 쌓던 세 섹션을 없앤다 — 한 화면에 들어갈 양을 스크롤로 나눌 이유가 없다.
- **오른쪽 = 사용처** 표 (형태 · 좌표). 건수는 「N / 전체 참조 M」 분모와 함께 (§9.6). 0건이면 「아무 문면 · 식도 이 구분자를 읽지 않는다」
  + 「문면에서 쓰러 가기」. 이 열은 읽기 전용이고 편집 모드에서도 그대로 보인다 — 고치는 동안 영향 범위를 옆에 둔다.
- **위험 구역 섹션은 없다.** 삭제는 헤더 오른쪽 끝 `.danger` 휴지통 아이콘(tooltip 「구분자 갱신여부(D0007) 삭제 — 저장된 값 N건이 사라진다」)
  → 확인 팝업. 타입 변경은 편집 모드의 타입 필드에서.

### 헤더 조작

| 모드 | 우측 상단 | 비고 |
|---|---|---|
| 읽기 | 「편집」 · 🗑 | 필드 전부 잠긴 칸 |
| 편집 | ✕ · 「저장」 · 🗑 | 저장은 변경이 있을 때만 활성. ✕ 는 변경 있으면 「계속 편집 / 버리기」 팝업 |
| 저장 중 | 전부 disabled | 끝나면 읽기. 거부되면 편집 유지 + 사유 배너 |

### 필드 — 종류별

| 필드 | 읽기 | 편집 | 비고 |
|---|---|---|---|
| 종류 | 단일값 · 폼 · 상수 · 파생 | 못 바꾼다 | 「스칼라」 는 어디에도 안 나온다 — 라벨 맵 |
| 레벨 ⓘ | 상품 · 세목 · 담보 · 세부보장 · 급부 | **지금은 못 바꾼다** (읽기 전용) — 서비스에 레벨 변경이 없다 (2026-09-06 확인). 액션 명세에는 있다 → Q-D | ⓘ 「이 구분자가 붙는 모델링의 층. 담보에 붙이면 담보마다 값을 갖는다」. 상수는 이 행이 없다 |
| 표시명 | 텍스트 | 입력 | 코드는 헤더에만, 잠김 |
| 설명 | 텍스트 | 여러 줄 입력 | |
| 노출 ⓘ | 무조건 · 선택 | 체크 「무조건 — 이 레벨의 모든 모델링에 값 자리를 만든다」 | ⓘ 「선택이면 담보 · 상품 화면에서 이 구분자를 붙인 곳에만 값 자리가 생긴다」. 「실체」 라는 말은 쓰지 않는다. 단일값 · 폼만 |
| 타입 | 「예/아니오」 · 「선택형 (고지유형)」 | 셀렉트 (+ 선택형이면 선택지 셀렉트). 바꾸면 기본값 칸이 새 타입의 입력기로 바뀌고 값은 비워진다 | 단일값만. 저장 시 저장된 값 N건 소실 확인 팝업 (§9.5) |
| 기본값 ⓘ | 값 또는 「없음」 | **타입을 따르는 입력기** — 아래 표 | 단일값만. ⓘ 「폼을 열 때 미리 채워 보여줄 제안. 사람이 저장해야 값이 된다」 |
| 값 | 텍스트 | 입력 | 상수만 |
| 식 | mono | mono 입력. 저장 전에 식 검사 | 파생만 |
| 필드 | 표 (코드 · 표시명 · 타입 · 기본값) | **여기서 안 고친다** — 「폼 탭에서 편집 →」 링크 ([[유형_화면기획]] §3) | 폼만 |

**기본값은 타입이 정한 입력기로 받는다** (2026-09-06 QA #4). 지금 구현은 타입과 무관한 자유 텍스트 칸이라 「무슨 값을 넣으라는 건지」가
안 보였다. [[01_기획/구분자_기획]] 의 전제(기본값 = 선택 항목 · 프리필 전용)는 유지하고, 입력기만 타입에 맞춘다.

| 타입 | 기본값 입력기 |
|---|---|
| 문자열 | 텍스트 |
| 숫자 | 숫자 (우측 정렬 · `tabular-nums`) |
| 예/아니오 | 셀렉트 「없음 · 예 · 아니오」 |
| 날짜 | 날짜 선택 |
| 선택형 | 그 선택지의 값 셀렉트 + 「없음」 |
| 선택형(복수) | 그 선택지의 값 다중 선택 |

타입이 아직 없는 자리(생성 폼)에는 기본값 칸을 두지 않는다 — 생성은 타입까지만 받고 기본값은 상세에서 넣는다.

### 조작과 상태 전이

| 조작 | 결과 |
|---|---|
| 편집 | 편집 모드. 왼쪽 필드가 입력기로 바뀐다. 사용처 열은 그대로 |
| 값 변경 | 「저장」 활성 |
| 저장 (타입 그대로) | 액션 「표시명 변경」 · 「설명 변경」 · 「노출여부 변경」 · 「기본값 지정 · 해제」 · 「const 값 변경」 · 「파생식 수정」 을 한 번에. 읽기 모드로 |
| 저장 (타입 바뀜) | 확인 팝업 「타입 바꾸고 값 N건 삭제」 → 확인하면 위 + 「타입 변경」. 취소하면 편집 모드 유지 |
| 저장 (레벨 바뀜) | 지금 없음 — 레벨은 읽기 전용 (Q-D) |
| ✕ | 변경 없으면 읽기 모드. 있으면 「계속 편집 / 버리기」 |
| 🗑 | 확인 팝업 (저장된 값 N건 · 사용처 M건). 사용처가 있으면 거부 + 사용처 목록 — 액션 「구분자 삭제」 |
| 사용처 행 클릭 | 그 좌표(문면 · 식)로 이동 |

### 예외 · 빈 상태 · 오류

- 없는 코드: 「찾을 수 없습니다」 + 목록 링크.
- 사용처 0건: 위 빈 상태 문구.
- 저장 거부(권한 · 중복 표시명 등): 편집 모드 유지 + 사유 배너. 입력한 값은 남는다.

### 2026-09-06 QA 반영 목록 (상세 화면)

| # | QA | 반영 |
|---|---|---|
| 1 | 사용처 · 위험 구역 섹션이 굳이 필요 없다. 좌우로 나누고 오른쪽에 사용처만 | 좌 = 기본 정보 · 우 = 사용처. 위험 구역 폐지 — 삭제는 헤더 아이콘, 타입 변경은 편집 모드 필드 |
| 2 | 종류에 「스칼라」 가 들어가지 않게 | 라벨 맵 「단일값」 (§1 과 같은 결정) |
| 3 | 「레벨」 · 「실체」 가 어색하다. 실체 → 모델링, ⓘ tooltip 설명 | 「실체」 는 화면에서 금지 → 「모델링」. 레벨 · 노출 · 기본값에 ⓘ tooltip. 앱 전체 규칙으로 [[02_UI/디자인원칙]] §2 L2 「화면 단어」 |
| 4 | 타입도 안 정했는데 기본값을 어떻게 넣나 | 기본값 입력기는 타입을 따른다. 생성 폼에는 기본값 없음. 기획 전제(프리필 전용)는 유지 |
| 5 | 읽기 모드 기본 · 우측 상단 편집 → 저장 · 변경 시 활성 · 로딩 중 disabled — 앱 전체 수정 화면 공통 | [[02_UI/디자인원칙]] §2 L2 「편집 흐름」 으로 올림. 이 화면이 첫 적용 |

### 인수기준
> [[05_QA/인수기준]] 에 등록 (미등록)

- 들어가면 모든 필드가 잠긴 칸이고 「편집」 을 눌러야 입력기가 된다. 「저장」 은 값을 바꾸기 전엔 비활성이다.
- 예/아니오 타입 구분자의 기본값 칸은 「없음 · 예 · 아니오」 셀렉트다. 자유 텍스트가 아니다.
- 화면 어디에도 「실체」 · 「스칼라」 · 「위험 구역」 이 없다. 레벨 · 노출 · 기본값 라벨 옆에 ⓘ 가 있고 hover 하면 한 줄 설명이 뜬다.
- 사용처가 왼쪽 필드와 같은 높이에 나란히 보인다 (스크롤 없이).

## 3. 생성 화면 (`/catalog/new`)

> 뼈대. 조회에서 넘어온 필터 값을 프리필한다 (§9.1). 종류 = 폼의 생성 폼은 [[유형_화면기획]] §4 와 같은 컴포넌트.
> 생성은 타입까지만 받는다 — 기본값 칸 없음 (§2).

## 4. enum 사전 (`/catalog/enums`)

> **폐지** (2026-09-06) → [[유형_화면기획]] §1 선택지 조회로 옮긴다.

## 열어 둔 질문

- [x] ~~Q-A. enum 사전의 진입 경로~~ → **2026-09-06 확정**: 선택지(enum) · 구조체를 관리하는 **별도 탭** [[유형_화면기획]].
      탭 이름은 그 문서의 Q-A.
- [ ] **Q-D. 레벨(부착 레벨) 변경 경로가 없다** (2026-09-06 구현에서 드러남). [[02_UI/유저액션/구분자_액션]] 에는 「부착 레벨 변경」
      액션이 있는데 서비스에 함수가 없어 화면은 읽기 전용으로 뒀다. 후보 — ⓐ 서비스에 추가(새 레벨 밖 값 행 삭제 + 확인)
      ⓑ 액션 명세에서 지우고 「레벨이 다르면 새로 채번」(〔D-P1-11〕과 같은 결)으로 닫는다.
- [x] ~~Q-B. `scalar` 의 화면 라벨~~ → **2026-09-06 확정**: 「단일값」. 「일반」 은 폼이 아닌 것으로 읽히기엔 흐리다.
      같은 자리에서 `struct` 의 화면 라벨도 「구조체」 → **「폼」** 으로 확정 (도메인 용어는 구조체 유지).
- [ ] **Q-C. 컬럼 필터의 복수 선택.** 지금은 컬럼당 값 하나. 「담보 또는 세부보장」 처럼 두 값이 필요해지면 칩 하나에
      값 여럿을 담을지, 칩을 값마다 낼지.
