# 아키텍처

> 상태: 초안 · 최종수정: 2026-09-05
> 2차 구현(2026-09-04~05, [[04_실행/2차구현_계획]])의 결과를 정리한 것. 코드가 정본이고 이 문서는 지도다.

## 전체 구조

```
src/domain/     순수층 — DB·React·Next import 금지 (ESLint 강제). 규칙은 전부 여기.
  types.ts        공유 계약: Code · ValueSlot · AttachLevel · Actor · Coordinate · Issue · Rejection · Result
  expression/     식 언어 한 벌 (ADR-0013) → [[식언어]]
  catalog/        구분자·구조체·enum·const·파생 정의, 코드 채번, 값 규칙, 파괴적 변경 영향
  auth/           역할 규칙 (ADR-0019) — 파괴적 액션 목록, destructive() 2단 프로토콜
  coverage/       담보 트리, 부착, 담보 레벨 값, 완결성, 마스터 평가 문맥
  clause/         공용조항 정의·옵션·요구 구분자 추출·참조 검사·인라인화
  document/       문면 노드 트리(ADR-0012), 편집 커맨드, 검증, 번호 계산, 참조 수집, 사전평가, 별표
  product/        담보속성, 상품, 세목, 탑재 스냅샷, 작명, 기본계약, 특약 그룹, 옵션 오버라이드
  refs/           참조 그래프 — 사용처 역인덱스, 고아·순환·깨진 간선, 관계정보 뷰
  assembly/       조립 파이프라인 — 문맥 → 해소 → 인라인화 → 치환 → 생략 → 번호 → 참조 → 별표 → 배치
src/db/
  schema/         drizzle 테이블 (영역별 파일) → [[데이터모델]]
  repo/           drizzle 쿼리만. 규칙 없음
  test-utils.ts   인메모리 PGlite + pushSchema (테스트 파일마다 새 DB)
src/services/   애플리케이션 서비스 — actor 첫 인자, 트랜잭션, 도메인 규칙 + repo. 모든 쓰기의 진입점
  container.ts    조립 루트 — 주입 인터페이스를 실제 구현으로 연결해 서비스 한 벌을 만든다
  txContext.ts    AsyncLocalStorage 기반 Db 프록시 — 열린 트랜잭션을 자동으로 탄다
src/forms/      「폼 = 구조체」 렌더러 (ADR-0001) — 순수 모델·리듀서 + React 컴포넌트
src/app/        Next.js 라우트·서버 액션 — 서비스 호출만, 규칙 없음
```

## 경계와 책임

- **규칙은 도메인에, 저장은 repo 에, 조합은 서비스에.** 서비스는 `(actor, …) → Result<T>` 를 돌려주고,
  거부는 `Rejection` 6종(forbidden · duplicate · minimumStructure · needsConfirmation · invalid · notFound)으로만 표현한다.
  화면은 거부를 표시할 뿐 판단하지 않는다 — 「숨김 ≠ 잠금」.
- **파괴적 액션은 한 프로토콜**(`destructive()`): assertCan(editor 거부) → precheck(최소 구조·notFound) →
  confirm 없으면 `needsConfirmation(Impact)` → confirm 이면 실행. 모든 영역이 같은 함수를 쓴다.
- **영역 간 결합은 주입 인터페이스로 끊는다.** 각 영역 서비스는 다른 영역을 import 하지 않고
  인터페이스(ImpactSource · UsageSource · ClauseGate · TypeResolver · CoverageMasterSource ·
  GeneralAttachmentCheck · OptionValidator · AttributeRefSource · Attacher)를 받는다.
  `container.ts` 가 유일하게 전부를 알고 연결한다. 순환 의존은 지연 참조.
- **참조는 그래프다.** refs 가 카탈로그·공용조항·문서·부착·조합·오버라이드에서 간선을 만들고,
  사용처 역조회·영향(Impact.brokenRefs)·관계정보·무결성을 낸다. 참조 추가 시점 검증은 각 영역이,
  삭제 영향·조립 시점 오류는 refs·assembly 가.
- **문맥은 언어 밖이다.** 식 언어는 `EvalContext`(lookup · attribute · children)를 주입받는다.
  담보 마스터 문맥(coverage.evalContext)·상품담보 스냅샷 문맥(assembly.context)이 각각 만든다.

## 데이터 흐름 — 조립 파이프라인 (ADR-0016)

| 단계 | 함수 | 중간 표현 |
|---|---|---|
| 1 문맥 구성 | `buildContexts` | `AssemblyContexts` — 상품담보마다 EvalContext(상품 값 + 스냅샷 값 + 담보속성 + const + 파생), 보통약관 = 상품 + 기본계약 담보 레벨 (ADR-0011) |
| 2·3 조건 해소 + 공용조항 인라인화 | `resolveDocument` | `ResolvedDoc` — 밟은 가지만 선택, 옵션은 오버라이드 > 마스터 (ADR-0010 · 0017) |
| 4 슬롯 치환 | `substituteSlots` | `SubstitutedDoc` |
| 5 생략 판정 | `judgeOmission` | + `OmissionRecord[]` — 조연결 조를 리터럴 비교 (ADR-0014) |
| 6 번호 계산 | `numberDocument` | `NumberedDoc` |
| 9 특약 배치 | `placeSpecials` | `Placement` — 그룹별, 담보 → 속성 종류 → 유효값 자동 정렬 |
| 8 별표 수집 | `collectAppendices` | `BookletAppendix[]` — 책자 전역 등장 순 |
| 7 참조 슬롯 해소 | `renderDocument` | `RenderedDoc` — 「제N조(조 명)」·「【별표N(이름)】」, 오류 마커 노드 포함 |

출력 `Booklet { general, specials, appendices, issues, complete, omitted, undocumented, trace }` 는 순수 데이터 —
Vitest 스냅샷 대상이자 산출물 회귀 비교([[00_시작/범위]] 유예)의 기반. 매번 재계산, 저장 없음.

## 트랜잭션 규칙 (PGlite)

PGlite 는 단일 연결이다. **트랜잭션 안에서 바깥 `db` 핸들로 쿼리하면 영구 대기**한다(2차 구현에서 두 번 실증).
서비스는 `db.transaction(tx => …)` 안에서 repo 에 반드시 `tx` 를 넘기고, 주입 소스는 조립 루트가 `contextualDb()` 프록시로
감싸 열린 tx 를 자동으로 타게 한다. 서버 Postgres 로 넘어가도 이 규칙은 해롭지 않다.

## 요구사항 (범위 문서에서)

- **다른 문서 종류(별지)가 같은 데이터에서 나와야 한다** — 문서 노드 종류는 규칙 테이블로 열려 있고,
  파이프라인 단계는 각각 순수 함수라 별지 파이프라인은 단계를 다시 조합하면 된다.
- **기본계약·보통약관을 단수로 하드코딩하지 않는다** (2026-09-04) — `product_base_contracts` 는 복수 허용,
  그룹 ↔ 보통약관 템플릿 자리가 있다. 「정확히 1개 / 전 그룹 동일 템플릿」은 검증 규칙이다.

## 관련 결정

[[06_결정/ADR-0009-테스트-분담]] · [[06_결정/ADR-0012-문면-노드트리-구조편집기]] · [[06_결정/ADR-0013-식언어-통일]] ·
[[06_결정/ADR-0016-조립-부분조립-오류좌표]] · [[06_결정/ADR-0019-모드폐지-역할권한]]
