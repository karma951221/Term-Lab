/**
 * 표현식(expression) 모듈 — placeholder.
 *
 * 약관 문면 안의 치환·조건·반복 표현식을 파싱하고 평가하는 순수 로직이 들어올 자리.
 * DB·React import 금지.
 */

/** 배선 확인용 트리비얼 함수. 실제 로직이 들어오면 삭제한다. */
export function isExpressionModuleReady(): boolean {
  return true;
}
