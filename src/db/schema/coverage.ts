/**
 * 담보 스키마 — 담보 > 세부보장 > 급부 계층과 각 층의 문면(文面).
 *
 * 여기 들어올 것 (P1 이후):
 * - 담보(coverage): 최상위 보장 단위
 * - 세부보장(sub-coverage): 담보 아래 구분자로 갈라지는 단위
 * - 급부(benefit): 실제 지급 사유·금액이 붙는 최소 단위
 * - 문면: 각 층에 매달리는 약관 텍스트(표현식 포함)
 *
 * 지금은 placeholder. 실제 테이블은 아직 정의하지 않는다.
 */

export {};
