/**
 * 카탈로그 스키마 — 구분자(discriminator), enum, 구조체(struct) 정의.
 *
 * 여기 들어올 것 (P1 이후):
 * - 구분자: 담보/급부를 가르는 축과 그 허용값 목록
 * - enum: 이름 있는 값 집합 (구분자 값의 단일 출처)
 * - 구조체: 폼 렌더러가 읽는 필드 스키마 (필드명·타입·제약)
 *
 * 지금은 placeholder. 실제 테이블은 아직 정의하지 않는다.
 */

export {};
